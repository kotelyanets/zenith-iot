[build]
  command = "npx prisma migrate deploy && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  # Ensure these match what's in your Netlify dashboard
  NODE_VERSION = "20"
